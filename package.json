{
  "name": "cov-bot",
  "version": "1.0.1",
  "description": "A Reddit bot accessing global data regarding the COVID-19 pandemic.",
  "main": "index.js",
  "scripts": {
    "start": "node dist/index.js",
    "postinstall": "tsc && cp env.json dist/env.json",
    "dev": "tsc && cp env.json dist/env.json && node dist/index.js",
    "deploy": "git add . && git add env.json --force && git commit -m Heroku && git push heroku master"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "cheerio": "^1.0.0-rc.3",
    "got": "^11.5.1",
    "request": "^2.88.2",
    "snoowrap": "^1.21.0",
    "string-similarity": "^4.0.1"
  },
  "devDependencies": {
    "@types/cheerio": "^0.22.22",
    "@types/string-similarity": "^4.0.0",
    "typescript": "^4.0.5"
  },
  "engines": {
    "node": "14.15.0"
  }
}
